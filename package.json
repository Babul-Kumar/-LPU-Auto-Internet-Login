{
  "name": "lpu-auto-internet-login",
  "version": "1.0.0",
  "description": "Smart Captive Portal Login Manager for LPU campus WiFi",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "zip": "node scripts/zip-dist.mjs",
    "release": "npm run build && npm run zip"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.29",
    "archiver": "^7.0.1",
    "terser": "^5.48.0",
    "vite": "^5.4.0"
  }
}
